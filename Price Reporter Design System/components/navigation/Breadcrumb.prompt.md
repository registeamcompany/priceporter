Small grey trail above article and service-page headings. The last item is never a link.

```jsx
<Breadcrumb items={[{label:'Home',href:'/'},{label:'GSA Contract',href:'/gsa'},{label:'Catalog upload'}]} />
```

The official PriceReporter lockups, straight from the brand guide artwork — never re-typeset or recolor them.

```jsx
<Logo height={32} />                        {/* horizontal, full color */}
<Logo variant="mark" height={28} />         {/* cube mark only */}
<Logo variant="stacked" tone="mono" />
```

The lockups are dark-text-on-light. On navy bands place the color lockup inside a white container, or use the mark alone next to a white "PRICEREPORTER" wordmark set in Roboto Medium.

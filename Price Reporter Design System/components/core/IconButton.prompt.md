Square icon-only control for toolbars, table row actions and app chrome.

```jsx
<IconButton icon="bell" label="Notifications" />
<IconButton icon="upload" label="Upload catalog" variant="solid" />
```

`label` is mandatory. Variants: `solid | soft | ghost | outline`.

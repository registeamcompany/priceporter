Pill for filters, SIN/category chips and applied search facets.

```jsx
<Tag active onClick={() => {}}>All contracts</Tag>
<Tag onRemove={() => {}}>SIN 33411</Tag>
```

Use `active` for the selected filter; `onRemove` turns it into a dismissible chip.

Single-choice control. Always give every Radio in a set the same `name`.

```jsx
<Radio name="plan" value="new" label="I need a new GSA contract" defaultChecked />
<Radio name="plan" value="manage" label="I already hold a contract" />
```

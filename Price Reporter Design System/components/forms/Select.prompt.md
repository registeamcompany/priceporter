Native select styled to match Input — schedules, SIN categories, order status filters.

```jsx
<Select label="Schedule" options={['MAS', 'VA Schedule', 'Other']} />
<Select size="sm" options={[{value:'all',label:'All statuses'}]} />
```
